# Execution Mode Configuration
# Options: "kwok" (simulated) or "real" (real workloads)
EXECUTION_MODE="real"

# Real clusters configuration (only used when MODE=real)
REAL_CLUSTERS_ENABLED=true

# KWOK configuration (only used when MODE=kwok)
KWOK_ENABLED=false
